'use client';import Navbar from '@/components/Navbar';import BottomNav from '@/components/BottomNav';import { useAuth } from '@/lib/auth';
export default function Profile(){const {user,logout}=useAuth();return(<><Navbar/><section className='container my-6'><div className='card'><h2 className='text-2xl font-extrabold'>الملف الشخصي</h2>{user?(<><p className='mt-2'><b>المستخدم:</b> {user.email}</p><p><b>نوع الحساب:</b> {user.accountType}</p><p><b>الدور:</b> {user.role}</p><button className='btn-ghost px-4 py-2 rounded-xl mt-2' onClick={logout}>تسجيل الخروج</button></>):<p className='text-slate-600 mt-2'>فضلاً سجّل الدخول.</p>}</div></section><BottomNav/></>)}