'use client';import Navbar from '@/components/Navbar';import BottomNav from '@/components/BottomNav';import { useStore } from '@/lib/store';import { useParams } from 'next/navigation';
export default function Details(){const {id}=useParams<{id:string}>();const c=useStore(s=>s.comps.find(x=>x.id===id));if(!c)return(<><Navbar/><div className='container my-6'><div className='card'>غير موجود</div></div></>);return(<><Navbar/><section className='container grid md:grid-cols-3 gap-4 my-6'><div className='card md:col-span-2'><h2 className='text-2xl font-extrabold'>{c.title}</h2><p className='text-slate-500'>{c.org} • {c.category}</p><p className='mt-2'>{c.brief}</p><div className='mt-4'><a className='btn' href={`/submit/${c.id}`}>قدّم مشاركتك</a></div></div><div className='card'><h3 className='font-extrabold'>المعايير</h3><ul className='list-disc pr-6 mt-2'>{c.rubric.map((r,i)=>(<li key={i}>{r.k} — {(r.w*100).toFixed(0)}%</li>))}</ul></div></section><BottomNav/></>)}