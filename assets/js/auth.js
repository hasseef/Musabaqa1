import { storage } from './state.js'; const AUTH_KEY='mus_auth'; const DEMO={user:'admin', pass:'M1234'}; export const getAuth=()=>storage.read(AUTH_KEY); export const setAuth=v=>{storage.write(AUTH_KEY,v);dispatchEvent(new CustomEvent('authchange',{detail:v}))}; export const clearAuth=()=>{storage.del(AUTH_KEY);dispatchEvent(new CustomEvent('authchange',{detail:null}))}; export const labelRole=r=>r==='participant'?'مشارك':r==='judge'?'محكّم':'مدير'; export const labelAccount=t=>({individual:'الأفراد',sponsor:'الرعاة',government:'الجهات الحكومية',nonprofit:'الجهات غير الربحية',company:'الشركات',investor:'المستثمرين'})[t]??t; export function initAuthUI(){ const modal=document.getElementById('authModal'); const authBtn=document.getElementById('authBtn'); const email=document.getElementById('email'); const password=document.getElementById('password'); const role=document.getElementById('role'); const accountType=document.getElementById('accountType'); const roleNav=document.getElementById('roleNav'); const roleInfo=document.getElementById('roleInfo'); const judgeTab=document.getElementById('judgeTab'); const adminAdvTab=document.getElementById('adminAdvTab'); const prizeTab=document.getElementById('prizeTab'); const walletTab=document.getElementById('walletTab'); const sponsorTab=document.getElementById('sponsorTab'); const marketTab=document.getElementById('marketTab'); const reportsTab=document.getElementById('reportsTab'); function upd(u){ if(u){ authBtn.textContent=`مرحبًا، ${u.email} (${labelAccount(u.accountType)} • ${labelRole(u.role)})`; authBtn.onclick=()=>{ if(confirm('تسجيل الخروج؟')) clearAuth() }; roleNav.hidden=false; roleInfo.textContent=`الحساب: ${labelAccount(u.accountType)} • الدور: ${labelRole(u.role)}`; judgeTab.hidden=u.role!=='judge'&&u.role!=='admin'; adminAdvTab.hidden=u.role!=='admin'; prizeTab.hidden=false; walletTab.hidden=false; sponsorTab.hidden=!(u.accountType==='sponsor'||u.role==='admin'); marketTab.hidden=!(u.accountType==='sponsor'||u.role==='admin'); reportsTab.hidden=!(u.accountType==='sponsor'||u.role==='admin'); } else { authBtn.textContent='دخول / تسجيل'; authBtn.onclick=()=>modal.showModal(); roleNav.hidden=true; } } upd(getAuth()); addEventListener('authchange',e=>upd(e.detail)); document.getElementById('quickLogins')?.addEventListener('click',e=>{ const b=e.target.closest('button[data-role]'); if(!b) return; const r=b.dataset.role; email.value=DEMO.user; password.value=DEMO.pass; role.value=r; }); document.getElementById('authForm')?.addEventListener('submit',ev=>{ ev.preventDefault(); if((email.value==='admin'||email.value==='admin@demo')&&password.value===DEMO.pass){ setAuth({email:'admin',role:role.value,accountType:accountType.value}); modal.close(); location.hash=role.value==='admin'?'#/admin-adv':role.value==='judge'?'#/judge':'#/dashboard'; return; } setAuth({email:email.value,role:role.value,accountType:accountType.value}); modal.close(); location.hash=role.value==='admin'?'#/admin-adv':role.value==='judge'?'#/judge':'#/dashboard'; }); document.querySelector('.modal__close')?.addEventListener('click',()=>modal.close()); }