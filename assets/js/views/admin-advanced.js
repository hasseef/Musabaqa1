import { comps, wallet, computeFee } from '../data.js'; export default function AdminAdvanced(){ const cs=comps(); const w=wallet(); const invs=w.invoices||[]; const fee=cs.reduce((s,c)=>s+computeFee(c.budget,c.feeRate),0); return `<section class='grid'><div class='card'><h2>لوحة المدير المتقدمة</h2><div class='kpis'><div class='kpi'><strong>${cs.length}</strong><span>المسابقات</span></div><div class='kpi'><strong>${w.balance}</strong><span>الرصيد</span></div><div class='kpi'><strong>${invs.length}</strong><span>فواتير</span></div><div class='kpi'><strong>${fee}</strong><span>عمولة متوقعة</span></div></div></div></section>`; }