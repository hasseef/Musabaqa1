import { comps } from '../data.js'; function qRender(q){ if(q.type==='radio'){ return `<div class='card'><p>${q.title}</p>${q.options.map((o,i)=>`<label><input type='radio' name='${q.id}' value='${o}' ${i===0?'checked':''}> ${o}</label>`).join('<br>')}</div>`;} if(q.type==='select'){ return `<div class='card'><p>${q.title}</p><select name='${q.id}' class='input'>${q.options.map(o=>`<option>${o}</option>`).join('')}</select></div>`;} return ''; } export default function Submit([id]){ const c=comps().find(x=>x.id===id); if(!c) throw new Error('غير موجود'); const mcq=(c.form||[]).map(qRender).join('')||'<p class=muted>لا توجد أسئلة إضافية.</p>'; const gate=c.requiresCode?`<label>رمز الوصول<input class='input' name='code' required></label>`:''; return `<section class='grid'><div class='card'><h2>تقديم — ${c.title}</h2><form id='subm' class='grid'><label>عنوان<input class='input' name='title' required></label><label>ملخص<textarea name='summary' required></textarea></label><label>رابط فيديو (اختياري)<input class='input' name='video' type='url'></label><label>ملصقات (اختياري)<input class='input' name='tags' placeholder='🇸🇦,🌟'></label>${gate}<div class='card'><h3>أسئلة اختيار</h3>${mcq}</div><button class='btn'>إرسال</button></form></div></section><script type='module'>import {{ comps }} from '../data.js'; const form=document.getElementById('subm'); const comp=comps().find(x=>x.id="${'{'}id{'}'}"); form.addEventListener('submit',e=>{{ e.preventDefault(); const fd=new FormData(form); if(comp.requiresCode && (fd.get('code')||'').trim()!==(comp.code||'')){{ alert('رمز الوصول غير صحيح'); return; }} alert('تم الإرسال'); location.hash='#/dashboard'; }});</script>`;}