import { comps } from '../data.js'; export default function Details([id]){ const c=comps().find(x=>x.id===id); if(!c) throw new Error('غير موجود'); return `<section class='grid'><div class='card'><h2>${c.title}</h2><p class='muted'>${c.org} • ${c.category}</p><p>${c.brief}</p><a class='btn' href='#/submit/${c.id}'>قدّم مشاركتك</a></div><div class='card'><h3>المعايير</h3><ul>${c.rubric.map(r=>`<li>${r.k} — ${(r.w*100).toFixed(0)}%</li>`).join('')}</ul></div></section>`;}