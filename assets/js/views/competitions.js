import { comps } from '../data.js'; export default function Competitions(){ const list=comps(); const cards=list.map(c=>`<div class='card'><h3>${c.title}</h3><p class='muted'>${c.org} • ${c.category}</p><p>${c.brief}</p><a class='btn' href='#/submit/${c.id}'>قدّم الآن</a> <a class='btn btn--light' href='#/details/${c.id}'>التفاصيل</a></div>`).join(''); return `<section class='grid grid-3'>${cards}</section>`;}